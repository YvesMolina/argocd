apiVersion: v1
kind: Service
metadata:
  name: myapp
spec:
  selector:
    app: myapp
  ports:
    - port: {{ .Values.app.ports.http }}
      targetPort: {{ .Values.app.ports.http }}
  type: ClusterIP
